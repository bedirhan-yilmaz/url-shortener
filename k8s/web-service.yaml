apiVersion: v1
kind: Service
metadata:
  name: url-shortener-web-service
  namespace: url-shortener
spec:
  selector:
    app: url-shortener-web
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
